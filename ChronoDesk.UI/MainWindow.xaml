<Window x:Class="ChronoDesk.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ChronoDesk.UI.ViewModels"
        xmlns:views="clr-namespace:ChronoDesk.UI.Views"
        mc:Ignorable="d"
        Title="ChronoDesk" Height="600" Width="900" 
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:TimerViewModel}">
            <views:TimerView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProjectListViewModel}">
            <views:ProjectListView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SummaryViewModel}">
            <views:SummaryView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <Border Grid.Column="0" Background="#333">
            <StackPanel Margin="10">
                <TextBlock Text="ChronoDesk" Foreground="White" FontSize="20" FontWeight="Bold" Margin="10,0,0,20"/>
                
                <Button Content="Timer" Command="{Binding NavigateToTimerCommand}" Margin="0,5" Padding="10" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left"/>
                <Button Content="Projects" Command="{Binding NavigateToProjectsCommand}" Margin="0,5" Padding="10" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left"/>
                <Button Content="Summary" Command="{Binding NavigateToSummaryCommand}" Margin="0,5" Padding="10" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left"/>
                <Button Content="Settings" Command="{Binding NavigateToSettingsCommand}" Margin="0,5" Padding="10" Background="Transparent" Foreground="White" BorderThickness="0" HorizontalContentAlignment="Left"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentViewModel}" Margin="10"/>
    </Grid>
</Window>
