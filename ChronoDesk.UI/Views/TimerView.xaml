<UserControl x:Class="ChronoDesk.UI.Views.TimerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Timer" Style="{StaticResource H1}"/>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="40" HorizontalAlignment="Center" VerticalAlignment="Center" Width="600">
            <StackPanel>
                <!-- Project Selection -->
                <TextBlock Text="Select Project" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                <ComboBox ItemsSource="{Binding Projects}" 
                          SelectedItem="{Binding SelectedProject}" 
                          DisplayMemberPath="Name" 
                          Height="40"
                          Margin="0,0,0,30"/>
                
                <!-- Timer Display -->
                <TextBlock Text="{Binding CurrentDuration}" 
                           FontSize="96" 
                           FontWeight="Light" 
                           HorizontalAlignment="Center" 
                           Foreground="{StaticResource TextBrush}"
                           Margin="0,0,0,30"
                           FontFamily="Segoe UI Light">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsTimerRunning}" Value="True">
                                    <DataTrigger.EnterActions>
                                        <BeginStoryboard x:Name="PulseStoryboard" Storyboard="{StaticResource PulseAnimation}"/>
                                    </DataTrigger.EnterActions>
                                    <DataTrigger.ExitActions>
                                        <StopStoryboard BeginStoryboardName="PulseStoryboard"/>
                                    </DataTrigger.ExitActions>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                
                <!-- Notes -->
                <TextBlock Text="What are you working on?" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                <TextBox Text="{Binding CurrentNotes}" 
                         Height="80" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True" 
                         VerticalContentAlignment="Top"
                         Margin="0,0,0,40"/>

                <!-- Actions -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Content="Start Timer" 
                            Command="{Binding StartCommand}" 
                            Style="{StaticResource PrimaryButton}"
                            Height="50"
                            Margin="0,0,10,0"
                            FontSize="16">
                         <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                     <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="&#xE768;" FontFamily="Segoe MDL2 Assets" Margin="0,0,10,0"/>
                                        <ContentPresenter/>
                                    </StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.9"/>
                                    </Trigger>
                                     <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.3"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    
                    <Button Grid.Column="1" 
                            Content="Stop Timer" 
                            Command="{Binding StopCommand}" 
                            Style="{StaticResource DangerButton}"
                            Height="50"
                            Margin="10,0,0,0"
                            FontSize="16">
                         <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                     <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="&#xE71A;" FontFamily="Segoe MDL2 Assets" Margin="0,0,10,0"/>
                                        <ContentPresenter/>
                                    </StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.9"/>
                                    </Trigger>
                                     <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.3"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
                
                <Button Content="Save Notes" Command="{Binding UpdateNotesCommand}" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Center" Margin="0,20,0,0" Width="120"/>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
