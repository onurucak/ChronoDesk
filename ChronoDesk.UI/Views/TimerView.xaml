<UserControl x:Class="ChronoDesk.UI.Views.TimerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            
            <TextBlock Text="Current Timer" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <ComboBox ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedProject}" DisplayMemberPath="Name" Width="300" Margin="0,0,0,20" Padding="5">
            </ComboBox>
            
            <TextBlock Text="{Binding CurrentDuration}" FontSize="60" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            
            <TextBox Text="{Binding CurrentNotes}" Width="300" Height="60" AcceptsReturn="True" Margin="0,0,0,10" Padding="5"/>
            <Button Content="Update Notes" Command="{Binding UpdateNotesCommand}" Width="100" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Start" Command="{Binding StartCommand}" Width="100" Height="40" Background="Green" Foreground="White" Margin="10"/>
                <Button Content="Stop" Command="{Binding StopCommand}" Width="100" Height="40" Background="Red" Foreground="White" Margin="10"/>
            </StackPanel>
            
        </StackPanel>
    </Grid>
</UserControl>
