<Window x:Class="ChronoDesk.UI.Views.EditSessionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edit Session" Height="750" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        WindowStyle="ToolWindow"
        Background="#1E1E1E">
    
    <Window.Resources>
        <!-- Only keep window-specific overrides if absolutely necessary. 
             Global styles in App.xaml should handle the rest. -->
        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="Foreground" Value="#DDDDDD"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Title -->
            <RowDefinition Height="*"/>    <!-- Content (Scrollable if needed) -->
            <RowDefinition Height="Auto"/> <!-- Buttons -->
        </Grid.RowDefinitions>

        <TextBlock Text="Edit Session" FontSize="24" FontWeight="Bold" Margin="0,0,0,30" Foreground="White"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Project -->
                <TextBlock Text="Project"/>
                <ComboBox ItemsSource="{Binding Projects}" 
                          SelectedItem="{Binding SelectedProject}"
                          DisplayMemberPath="Name"
                          Height="40"/>

                <!-- Start Time -->
                <TextBlock Text="Start Time"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>
                    <DatePicker Grid.Column="0" SelectedDate="{Binding StartDate}" Margin="0,0,15,20" Height="40" VerticalContentAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding StartTimeStr}" ToolTip="HH:mm (e.g. 14:30)" Height="40" VerticalContentAlignment="Center"/>
                </Grid>

                <!-- End Time -->
                <TextBlock Text="End Time"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>
                    <DatePicker Grid.Column="0" SelectedDate="{Binding EndDate}" Margin="0,0,15,20" Height="40" VerticalContentAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding EndTimeStr}" ToolTip="HH:mm (e.g. 16:00)" Height="40" VerticalContentAlignment="Center"/>
                </Grid>

                <!-- Notes -->
                <TextBlock Text="Notes"/>
                <TextBox Text="{Binding Notes}" Height="150" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Actions -->
        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,30,0,0">
             <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap" Margin="0,0,0,15" 
                       Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"/>
             
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Command="{Binding CancelCommand}" 
                        Width="100" Height="40" Margin="0,0,15,0" Background="Transparent" Foreground="White" BorderBrush="#555555" FontSize="14"/>
                <Button Content="Save" Command="{Binding SaveCommand}" 
                        Width="120" Height="40" Background="#007ACC" Foreground="White" BorderThickness="0" FontSize="14" FontWeight="SemiBold"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
