<UserControl x:Class="ChronoDesk.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Settings" FontSize="28" FontWeight="Bold" Margin="0,0,0,30"/>

        <Grid Grid.Row="1" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="300"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*" MinWidth="300"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Preferences -->
            <Border Style="{StaticResource CardStyle}" Padding="30" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Preferences" FontWeight="SemiBold" FontSize="18" Margin="0,0,0,25"/>



                    <!-- Auto-save -->
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Left">
                             <TextBlock Text="Auto-save Timer"/>
                             <TextBlock Text="Save progress automatically every 30s" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13" Margin="0,4,0,0"/>
                        </StackPanel>
                        <CheckBox IsChecked="{Binding IsAutoSaveEnabled, Mode=TwoWay}" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{x:Null}">
                             <CheckBox.LayoutTransform>
                                <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                             </CheckBox.LayoutTransform>
                        </CheckBox>
                    </DockPanel>
                </StackPanel>
            </Border>

            <!-- About Card -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Padding="30" VerticalAlignment="Top">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,25">
                         <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets" FontSize="28" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,15,0" VerticalAlignment="Center"/>
                         <StackPanel>
                             <TextBlock Text="About ChronoDesk" FontWeight="SemiBold" FontSize="18"/>
                             <TextBlock Text="Time tracking made simple." Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                         </StackPanel>
                    </StackPanel>
                    
                    <Grid Margin="0,0,0,15">
                        <TextBlock Text="App Version" FontSize="15"/>
                        <TextBlock Text="{Binding Version}" HorizontalAlignment="Right" Foreground="{StaticResource TextSecondaryBrush}" FontSize="15"/>
                    </Grid>
                    <Grid>
                        <TextBlock Text="License" FontSize="15"/>
                        <TextBlock Text="Open Source (MIT)" HorizontalAlignment="Right" Foreground="{StaticResource TextSecondaryBrush}" FontSize="15"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Danger Zone -->
            <Border Grid.Row="1" Grid.ColumnSpan="3" Style="{StaticResource CardStyle}" Padding="30" Margin="0,30,0,0" BorderBrush="{StaticResource DangerBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel>
                         <TextBlock Text="Danger Zone" FontWeight="SemiBold" FontSize="18" Foreground="{StaticResource DangerBrush}" Margin="0,0,0,10"/>
                         <TextBlock Text="Clear all application data including projects and time logs." Foreground="{StaticResource TextSecondaryBrush}"/>
                         <TextBlock Text="This action cannot be undone." Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock HorizontalAlignment="Left" Margin="0,0,0,8" Opacity="0.7" Foreground="White" FontSize="13">
                            Type <Run Text="delete me" Foreground="{StaticResource DangerBrush}" FontWeight="SemiBold"/> to confirm
                        </TextBlock>
                        <StackPanel Orientation="Horizontal">
                            <TextBox Width="200" Margin="0,0,15,0" VerticalContentAlignment="Center" 
                                     Text="{Binding DeleteConfirmationText, UpdateSourceTrigger=PropertyChanged}"
                                     Tag="Type 'delete me' to confirm">
                                 <TextBox.Style>
                                    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                        <Style.Triggers>
                                            <Trigger Property="Text" Value="">
                                                <Setter Property="Background" Value="#252526"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                 </TextBox.Style>
                            </TextBox>
                            <Button Content="Clear All Data" Command="{Binding ClearDatabaseCommand}" Style="{StaticResource DangerButton}" Width="120"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>

        </Grid>
    </Grid>
</UserControl>
