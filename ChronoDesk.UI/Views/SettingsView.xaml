<UserControl x:Class="ChronoDesk.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Settings" FontSize="28" FontWeight="Bold" Margin="0,0,0,30"/>

        <StackPanel Grid.Row="1" MaxWidth="600" HorizontalAlignment="Left">
            
            <!-- About Card -->
            <Border Style="{StaticResource CardStyle}" Padding="25">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                         <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,15,0" VerticalAlignment="Center"/>
                         <StackPanel>
                             <TextBlock Text="About ChronoDesk" FontWeight="SemiBold" FontSize="16"/>
                             <TextBlock Text="Time tracking made simple." Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                         </StackPanel>
                    </StackPanel>
                    
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="App Version"/>
                        <TextBlock Text="{Binding Version}" HorizontalAlignment="Right" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </Grid>
                    <Grid>
                        <TextBlock Text="License"/>
                        <TextBlock Text="Open Source (MIT)" HorizontalAlignment="Right" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Preferences (Mock) -->
            <Border Style="{StaticResource CardStyle}" Padding="25" Margin="0,30,0,0">
                <StackPanel>
                     <TextBlock Text="Preferences" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,20"/>
                     
                     <Grid Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Dark Mode"/>
                            <TextBlock Text="Use system default logic" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                        </StackPanel>
                        <!-- Simple Toggle Switch Lookalike Checkbox -->
                        <CheckBox IsChecked="True" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{x:Null}">
                             <CheckBox.LayoutTransform>
                                <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                             </CheckBox.LayoutTransform>
                        </CheckBox>
                    </Grid>

                    <Grid>
                        <StackPanel>
                            <TextBlock Text="Auto-save Timer"/>
                            <TextBlock Text="Save progress every 30 seconds" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                        </StackPanel>
                        <CheckBox IsChecked="True" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{x:Null}">
                             <CheckBox.LayoutTransform>
                                <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
                             </CheckBox.LayoutTransform>
                        </CheckBox>
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>
