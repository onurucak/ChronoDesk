<UserControl x:Class="ChronoDesk.UI.Views.SummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Summary &amp; Reports" Style="{StaticResource H1}"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <DatePicker SelectedDate="{Binding StartDate}" Margin="0,0,10,0" VerticalContentAlignment="Center" Padding="5"/>
                <TextBlock Text="-" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextSecondaryBrush}"/>
                <DatePicker SelectedDate="{Binding EndDate}" Margin="0,0,20,0" VerticalContentAlignment="Center" Padding="5"/>
                
                <Button Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0">
                     <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" Margin="0,0,8,0"/>
                        <TextBlock Text="Refresh"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding ExportCommand}" Style="{StaticResource PrimaryButton}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE78C;" FontFamily="Segoe MDL2 Assets" Margin="0,0,8,0"/>
                        <TextBlock Text="Export CSV"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Stats Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Style="{StaticResource CardStyle}" Margin="0,0,10,0" Padding="20">
                <StackPanel>
                    <TextBlock Text="Total Projects" Foreground="{StaticResource TextSecondaryBrush}" FontSize="14"/>
                    <TextBlock Text="{Binding ProjectSummaries.Count}" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
                
            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="10,0,10,0" Padding="20">
                    <StackPanel>
                    <TextBlock Text="Active Period" Foreground="{StaticResource TextSecondaryBrush}" FontSize="14"/>
                    <TextBlock Text="7 Days" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
            
            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="10,0,0,0" Padding="20">
                    <StackPanel>
                    <TextBlock Text="Status" Foreground="{StaticResource TextSecondaryBrush}" FontSize="14"/>
                    <TextBlock Text="Productive" FontSize="32" FontWeight="Bold" Margin="0,10,0,0" Foreground="{StaticResource SuccessBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Project Breakdown (Scrollable) -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
            <Border Style="{StaticResource CardStyle}" Padding="0" Margin="0,0,0,20">
                <Grid MinHeight="250">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="20">
                        <TextBlock Text="Project Breakdown" Style="{StaticResource H2}" Margin="0"/>
                    </Border>
                
                    <ItemsControl Grid.Row="1" ItemsSource="{Binding ProjectBreakdown}" BorderThickness="0" Background="Transparent" Margin="0,10">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,0,20" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,20">
                                    <StackPanel Margin="20,0">
                                        <!-- Daily Header -->
                                        <Grid Margin="0,0,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Date, StringFormat='ddd, MMM dd'}" FontSize="16" FontWeight="SemiBold" Foreground="White" Margin="0,0,10,0"/>
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="{Binding TotalDuration, Converter={StaticResource TimeSpanToDurationConverter}}" FontFamily="Consolas" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}"/>
                                        </Grid>

                                        <!-- Nested Projects List -->
                                        <ItemsControl ItemsSource="{Binding Projects}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="10,5,0,5">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Orientation="Horizontal">
                                                            <Ellipse Width="6" Height="6" Fill="{StaticResource SurfaceLightBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                            <TextBlock Text="{Binding ProjectName}" FontSize="14" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                        <TextBlock Grid.Column="1" Text="{Binding TotalDuration, Converter={StaticResource TimeSpanToDurationConverter}}" FontFamily="Consolas" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Empty State -->
                    <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" 
                                Visibility="{Binding IsProjectBreakdownEmpty, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <!-- Icon: 'Library' or 'Collection' looking icon, or 'PieChart' -->
                            <Border Background="#1AFFFFFF" CornerRadius="50" Width="80" Height="80" Margin="0,0,0,20">
                            <TextBlock Text="&#xE9F9;" FontFamily="Segoe MDL2 Assets" FontSize="32" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="No activity recorded" FontWeight="SemiBold" FontSize="18" Foreground="{StaticResource TextBrush}" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="Start tracking time to see your breakdown here." Foreground="{StaticResource TextSecondaryBrush}" FontSize="14" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" 
                                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"
                                HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20"/>
                </Grid>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
