<UserControl x:Class="ChronoDesk.UI.Views.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Projects" FontSize="28" FontWeight="Bold" Margin="0,0,0,30"/>

        <!-- Create Project Card -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="20" Margin="0,0,0,30">
            <StackPanel>
                <TextBlock Text="Create New Project" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,20"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Margin="0,0,15,0">
                        <TextBlock Text="Name" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <TextBox Text="{Binding NewProjectName, UpdateSourceTrigger=PropertyChanged}" Padding="10"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Margin="0,0,15,0">
                        <TextBlock Text="Description" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                         <TextBox Text="{Binding NewProjectDescription}" Padding="10"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                        <Button Content="Create Project" Command="{Binding CreateProjectCommand}" Style="{StaticResource PrimaryButton}" Width="140"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Project List -->
        <ListView Grid.Row="2" ItemsSource="{Binding Projects}" 
                  Background="Transparent" 
                  BorderThickness="0" 
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,10" Padding="15">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/> <!-- Icon -->
                            <ColumnDefinition Width="*"/>    <!-- Info -->
                            <ColumnDefinition Width="Auto"/> <!-- Actions -->
                        </Grid.ColumnDefinitions>
                        
                        <Border Width="40" Height="40" Background="#333333" CornerRadius="20" Margin="0,0,15,0" VerticalAlignment="Center">
                            <TextBlock Text="&#xE8F1;" FontFamily="Segoe MDL2 Assets" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding Description}" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </StackPanel>
                        
                        <Button Grid.Column="2" 
                                Content="Archive" 
                                Command="{Binding DataContext.ArchiveProjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                CommandParameter="{Binding}"
                                Style="{StaticResource SecondaryButton}"
                                VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
