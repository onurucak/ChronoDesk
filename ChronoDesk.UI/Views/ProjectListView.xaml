<UserControl x:Class="ChronoDesk.UI.Views.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Projects" FontSize="28" FontWeight="Bold" Margin="0,0,0,30"/>

        <!-- Create Project Card -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="20" Margin="0,0,0,30">
            <StackPanel>
                <TextBlock Text="Create New Project" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,20"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Margin="0,0,15,0">
                        <TextBlock Text="Name" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                        <TextBox Text="{Binding NewProjectName, UpdateSourceTrigger=PropertyChanged}" Padding="10"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Margin="0,0,15,0">
                        <TextBlock Text="Description" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                         <TextBox Text="{Binding NewProjectDescription}" Padding="10"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                        <Button Content="Create Project" Command="{Binding CreateProjectCommand}" Style="{StaticResource PrimaryButton}" Width="140"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Project List -->
        <ListView Grid.Row="2" ItemsSource="{Binding Projects}" 
                  Background="Transparent" 
                  BorderThickness="0" 
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,10" Padding="15">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource CardStyle}" Margin="0,5" Padding="15">
                        <Grid>
                            <!-- Display Mode -->
                            <Grid Visibility="{Binding IsEditing, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock Text="{Binding Description}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="Edit" Command="{Binding EditCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0"/>
                                    <Button Content="Archive" Command="{Binding ArchiveCommand}" Style="{StaticResource DangerButton}"/>
                                </StackPanel>
                            </Grid>

                            <!-- Edit Mode -->
                            <Grid Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Edit Project" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                    <TextBox Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                                    <TextBox Text="{Binding EditDescription, UpdateSourceTrigger=PropertyChanged}" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
                                </StackPanel>
                                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                                    <Button Content="Save" Command="{Binding SaveEditCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"/>
                                    <Button Content="Cancel" Command="{Binding CancelEditCommand}" Style="{StaticResource SecondaryButton}"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
