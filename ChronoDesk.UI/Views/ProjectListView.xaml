<UserControl x:Class="ChronoDesk.UI.Views.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Projects" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Add New Project -->
        <Border Grid.Row="1" Background="#F0F0F0" Padding="15" CornerRadius="5" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBox Width="200" Text="{Binding NewProjectName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0" Padding="5" Tag="Name"/>
                <TextBox Width="300" Text="{Binding NewProjectDescription}" Margin="0,0,10,0" Padding="5" Tag="Description"/>
                <Button Content="Create Project" Command="{Binding CreateProjectCommand}" Padding="10,5"/>
            </StackPanel>
        </Border>

        <!-- Project List -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Projects}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*"/>
                <DataGridTemplateColumn Header="Actions" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Archive" Command="{Binding DataContext.ArchiveProjectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="5"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
